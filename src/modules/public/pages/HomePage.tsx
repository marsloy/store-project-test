import { ProductList } from "../components";
import { LayoutMain } from "../layout/LayoutMain";

export const HomePage = () => {
  return (
    <LayoutMain>
      <ProductList />
    </LayoutMain>
  );
};
